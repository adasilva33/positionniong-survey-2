{
  "_comment": "TEMPLATE ONLY. Real file must be named credentials.json (git-ignored). Get it from Google Cloud Console -> APIs & Services -> Credentials -> Create credentials -> OAuth client ID -> Application type: Desktop app -> Download JSON. Must be a Desktop-app client (top-level key 'installed'); a Web client causes redirect_uri_mismatch.",
  "installed": {
    "client_id": "REPLACE_ME.apps.googleusercontent.com",
    "project_id": "your-gcp-project-id",
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_uri": "https://oauth2.googleapis.com/token",
    "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "client_secret": "REPLACE_ME",
    "redirect_uris": ["http://localhost"]
  }
}
